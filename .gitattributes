# Auto-detect text files and normalize to LF in repo
* text=auto eol=lf

# Shell scripts — always LF (bash breaks on CRLF)
*.sh text eol=lf
*.bash text eol=lf
*.zsh text eol=lf

# PowerShell scripts — LF is fine (PS handles both)
*.ps1 text eol=lf
