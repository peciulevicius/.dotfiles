# ccmpiled stuff
config/nvim/plugin/packer_compiled.lua
site/

# claude settings
.claude/settings.local.json

# Self-hosted services — never commit secrets or data
services/**/.env
services/**/.env.deployed
services/**/data/
services/**/library/

# Claude Code — machine-specific permissions
.claude/settings.local.json

# Obsidian vault has its own repo
obsidian-vault/

# PKM — config.py has credentials, track config.example.py only
pkm/config.py
pkm/.venv/
pkm/.processed_ids

# Backup logs
logs/
